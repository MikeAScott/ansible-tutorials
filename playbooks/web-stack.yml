---
- name: Gather facts
  hosts: all
  gather_facts: true

- name: Configure DB
  hosts: localhost
  vars_files:
    - secrets.yml
  roles:
    # - role: mysql-server
    - role: get-dist-from-s3
    - role: todolist-db

- name: Configure Web Server
  hosts: web
  become: true
  vars_files:
    - secrets.yml

  # vars:
  #   moved to group_vars file 

  roles:
    - role: tomcat-server
    - role: todo-webapp


        
